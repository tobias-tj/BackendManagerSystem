spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/projectsdb
    username:
    password:
  jpa:
    database-platform: org.hibernate.dialect.PosgreSQLInnoDBDialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        globally_quoted_identifiers: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: "*********"
    password: "*********"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server:
  port: 8080
  error:
    path: /user/error
    whitelabel:
      enabled: false

stripe:
  apiKey: DeKeyTest121232342
